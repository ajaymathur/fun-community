import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    loginPanel: {
        flex: 1,
        padding: 10,
    },
    errorMessage: {
        color: 'red',
        marginBottom:15
    },
    inputBox: {
        left: -15,
    }
})

export default styles;
